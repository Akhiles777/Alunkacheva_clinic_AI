-- Метка браузера в журнале действий.
--
-- Два одинаковых iPhone дают одну строку браузера, и в /sistem их было не
-- различить. Колонка только добавляется и может быть пустой: прошлые строки
-- журнала не меняются, ничего не удаляется и не переименовывается.
ALTER TABLE "audit_logs" ADD COLUMN IF NOT EXISTS "deviceId" TEXT;
