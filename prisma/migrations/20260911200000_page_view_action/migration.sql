-- Новый вид действия в журнале: открытие экрана.
--
-- Только добавление значения в перечисление: существующие строки не меняются,
-- ничего не удаляется и не переименовывается.
ALTER TYPE "AuditAction" ADD VALUE IF NOT EXISTS 'PAGE_VIEW';
