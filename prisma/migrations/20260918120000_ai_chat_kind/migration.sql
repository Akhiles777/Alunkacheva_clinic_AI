-- Вид разговора с ассистентом: разбор владельца или чат администратора.
--
-- Только добавление колонки со значением по умолчанию: существующие разговоры
-- остаются разборами владельца, ничего не удаляется и не переименовывается.
ALTER TABLE "ai_chats" ADD COLUMN IF NOT EXISTS "kind" TEXT NOT NULL DEFAULT 'OWNER';
