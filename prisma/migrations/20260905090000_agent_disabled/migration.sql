-- Агент, выключенный в диалоге насовсем.
--
-- Пауза после перехвата истекает сама — правильно для пациента, который ждёт
-- ответа. Но в пациентский канал пишут и сотрудники клиники между собой
-- («придёт Гулбарият, взять ОАК, оплату не брать»), и агент отвечает им как
-- пациенту. Отличить сотрудника от пациента он не может, а человек может:
-- решение отдано человеку, и срок у него не истекает.
ALTER TABLE "conversations" ADD COLUMN "agentDisabled" BOOLEAN NOT NULL DEFAULT false;
