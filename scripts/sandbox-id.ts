/**
 * Номер клиники-песочницы. Отдельным файлом, потому что его знают трое:
 * тот, кто разворачивает песочницу, тот, кто по ней гоняет диалоги, и тот,
 * кто её убирает. Разъедутся — прогон пойдёт не по той клинике.
 *
 * Ниже ста намеренно: планировщик выгружает клиники с `yclientsId >= 100`
 * (lib/server/scheduler). С прежним 999001 песочница попадала в каждый круг
 * выгрузки, и сервер ходил в YCLIENTS за несуществующим филиалом.
 */
export const SANDBOX_YCLIENTS_ID = 99;

/** Номера, под которыми песочница уже бывала: старый 999001 нужно уметь убрать. */
export const SANDBOX_YCLIENTS_IDS = [SANDBOX_YCLIENTS_ID, 999_001];
